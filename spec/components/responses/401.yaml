description: Unauthorized
headers:
    WWW-Authenticate:
content:
    application/json:
        schema:
            type: object
            properties:
                detail:
                    type: string
                    example: "Invalid authentication credentials supplied"