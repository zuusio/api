type: object
properties:
    object:
        type: string
        enum: ["allowance"]
    name:
        type: string
        description: Allowance condition name
        example: Daily Uniform Allowance
    value:
        type: number
        example: 1.25
required:
    - name
    - value
