type: object
properties:
    object:
        type: string
        enum: ["employmentperiod"]
    start:
        $ref: "#/components/schemas/date"
    finish:
        $ref: "#/components/schemas/date"
    reason:
        type: string
        description: Termination reason
