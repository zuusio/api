type: object
properties:
  object:
    type: string
    enum: ["entityperiod"]
  uuid:
    type: string
    format: uuid
  start:
    description: ISO 8601 Date
    type: string
    format: date
  finish:
    description: ISO 8601 Date
    type: string
    format: date
required:
  - uuid
