type: object
properties:
    object:
        type: string
        enum: ["entityperiod"]
    uuid:
        type: string
        format: uuid
    start:
        $ref: "#/components/schemas/date"
    finish:
        $ref: "#/components/schemas/date"
required:
    - uuid
