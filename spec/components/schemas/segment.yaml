type: object
properties:
  start:
    $ref: '#/components/schemas/datetime'
  finish:
    $ref: '#/components/schemas/datetime'
  job:
    type: string
    format: uuid
  notes:
    type: string
required:
  - start
