type: object
properties:
    object:
        type: string
        enum: ["shift"]
    uuid:
        type: string
        format: uuid
    staff:
        type: string
        format: uuid
    location:
        type: string
        format: uuid
    start:
        $ref: "#/components/schemas/datetime_timezone"
    finish:
        $ref: "#/components/schemas/datetime_timezone"
    segments:
        type: array
        items:
            $ref: "#/components/schemas/segment"
    allowance:
        type: array
        items:
            $ref: "#/components/schemas/allowance"
    notes:
        type: string
