type: object
properties:
  object:
    type: string
    enum: ["stat"]
  start:
    $ref: "#/components/schemas/datetime"
  finish:
    $ref: "#/components/schemas/datetime"
  value:
    type: number
