allOf:
  - $ref: '#/components/schemas/shift'
  - type: object
    properties:
      system:
        type: string
      exceptions:
        type: array
        items:
          type: object
          properties:
            manager:
              type: string
              format: uuid
            reason:
              type: string
            approvedAt:
              $ref: '#/components/schemas/datetime'
